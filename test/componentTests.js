// import expect from 'expect';
// import React from 'react';
// import { shallow } from 'enzyme';
// import Signup from '../client/app/stateless/Signup';

// // function setup() {
// //   const props = {
// //     username: expect.createSpy(),
// //     picUrl: expect.createSpy(),
// //     subreddits: expect.createSpy(),
// //     messageUrl: expect.createSpy(),
// //   };

// //   const enzymeWrapper = shallow(<Signup {...props} />);

// //   return {
// //     props,
// //     enzymeWrapper,
// //   };
// // }

// describe('components', () => {
//   describe('Signup', () => {
//     it('should render self and subcomponents', () => {
//       const wrapper = shallow(<Signup />);

//       expect(wrapper.find('h1').text()).toBe('Sign Up with your Reddit Account');
//       expect(wrapper.find('button')).to.have.length(1);
//       // expect(enzymeWrapper.find('header').hasClass('header')).toBe(true);

//       // const todoInputProps = enzymeWrapper.find('TodoTextInput').props();
//       // expect(todoInputProps.newTodo).toBe(true);
//       // expect(todoInputProps.placeholder).toEqual('What needs to be done?');
//     });

//     // it('should call addTodo if length of text is greater than 0', () => {
//     //   const { enzymeWrapper, props } = setup();
//     //   const input = enzymeWrapper.find('TodoTextInput');
//     //   input.props().onSave('');
//     //   expect(props.addTodo.calls.length).toBe(0);
//     //   input.props().onSave('Use Redux');
//     //   expect(props.addTodo.calls.length).toBe(1);
//     // });
//   });
// });
